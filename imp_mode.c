#include "monty.h"
/**
 * unique_addst - A function that changes mode of push to stack
 * @structure: pointer to stack
 * @line_number: line number
 * Return: Absolutely Nothing.
 */
void unique_addst(stack_t **structure, unsigned int line_number)
{
(void)line_number;
(void)structure;
arg.flag = 0;
}
/**
 * unique_addqu - A function that changes mode of push to queue
 * @structure: pointer to stack
 * @line_number: line number
 * Return: Absolutely Nothing.
 */
void unique_addqu(stack_t **structure, unsigned int line_number)
{
(void)line_number;
(void)structure;
arg.flag = 1;
}
